# V1 发布规范

本文档是 Landing Page Design System 的发布基线与验收清单。它用于判断一个版本是否可以作为稳定设计系统继续复用，而不是记录日常修改历史。

## 1. 当前发布定位

当前代码以 **V1 稳定基线** 为目标。页面信息架构、组件层级、状态、响应式规则与 CSS 职责已经完成结构化整理；正式对外标记版本前，仍需要完成真实浏览器视觉验收。

源码权威仓库：`1994sidney/landing-page-design-system`

发布页面：GitHub Pages，从 `main` 分支根目录发布。

本仓库独立维护，不自动合并到其他业务仓库，也不自动部署 Vercel。

## 2. 发布范围

V1 必须包含以下六个一级模块：

1. 设计基础：设计原则、色彩系统、字体样式、栅格与间距。
2. 基础组件：按钮与行动入口、标签与元信息、卡片、状态与反馈。
3. 数据图表：数据卡片、ECharts 图表组件。
4. 内容与媒体：图文内容、新闻 / 文章 / 案例、信任与证明、毛玻璃叠层、观点与引语、摄影与图片。
5. 流程与交互：动态流程场景、联系与表单、FAQ、动效语言。
6. 页面结构：导航栏、Hero、CTA、页脚、页面蓝图、内容与响应式规范、开发实现令牌。

一级模块使用 H2，二级组件组使用 H3，组件内部标题从 H4 开始。顶部导航只对应一级模块。

## 3. CSS 发布架构

正式样式分成两层：

- `styles/core.css`：Reset、全局布局骨架、字体与容器、当前静态 DOM 必需的基础结构、最小响应式与 Reduced Motion。
- `styles/system.css`：设计系统语义入口，按职责加载 `styles/modules/`。

`styles/base.css` 仅为当前 `index.html` 保留兼容转发，不允许继续向其中增加规则。新页面应直接引用 `core.css` 与 `system.css`。

禁止重新引入 `01.css / 02.css`、`v2.css`、`components-v18.js` 这类按开发顺序命名的历史文件。

### modules 职责

- `foundation.css`：主题变量、首屏视觉、设计基础、基础控件。
- `data.css`：数据卡片与关键指标。
- `charts.css`：图表容器与图表版式。
- `media.css`：图文、毛玻璃、观点与摄影。
- `content.css`：新闻、文章与案例。
- `trust.css`：合作机构、评价、团队与证据型内容。
- `process.css`：动态流程场景与动效语言。
- `forms.css`：联系表单与 FAQ。
- `navigation.css`：顶部一级模块导航。
- `rhythm.css`：唯一的页面纵向节奏变量来源。
- `hierarchy.css`：H2 / H3 / 组件三级层级与模块背景。
- `page-sections.css`：导航、Hero、CTA 与页脚变体。
- `states.css`：Hover / Focus / Disabled / Error / Loading / Empty 等状态。
- `guidelines.css`：内容长度、图片比例、响应式与极端内容边界。

## 4. JavaScript 发布架构

JavaScript 不负责重组页面 DOM。

- `script.js`：滚动显现、FAQ 交互、一级导航 Scroll Spy、图片失败占位。
- `charts.js`：ECharts 加载、图表初始化与图表状态。
- `process.js`：Iconify / Anime.js 流程场景增强。

新增页面结构必须直接写入 HTML。只有真实交互、异步可视化和动画增强可以进入 JavaScript。

## 5. 响应式基线

统一使用三层语义：

- Desktop：`>= 1180px`
- Tablet：`681–1179px`
- Mobile：`<= 680px`

复杂组件允许在 `980px` 左右提前发生内部重排。

发布前至少检查以下视口：

- 1440 × 900
- 1280 × 800
- 1024 × 768
- 768 × 1024
- 430 × 932
- 390 × 844
- 375 × 812

不得出现整页横向滚动、正文被裁切、按钮文字溢出、图表标签互相覆盖或固定高度截断核心内容。

## 6. 内容与图片验收

Hero 核心判断建议 14–28 个汉字；一级模块标题应保持分类名称而不是长句；按钮通常保持 2–8 个汉字。列表型内容可以行数截断，Hero、核心观点和详情标题不允许依靠省略号解决内容过长问题。

图片应根据角色选择比例。Hero 以横向环境图为主，图文内容常用 16:10，人物团队优先 4:5，Logo 必须使用 contain。所有内容图片必须提供有意义的 `alt`；纯装饰图形使用合适的隐藏方式。

远程图片失败时必须显示统一占位，不能暴露浏览器默认破图。

## 7. 交互与无障碍验收

发布前必须确认：

- 所有可点击按钮与链接存在可见 Hover / Focus 状态。
- 键盘 Tab 顺序符合页面阅读顺序。
- FAQ 使用真实 button，并同步 `aria-expanded`。
- Disabled、Error、Success、Warning、Loading、Empty、No data 不只依赖颜色表达。
- 输入错误提供文字原因。
- `prefers-reduced-motion: reduce` 下不依赖动画才能理解内容。
- 主要触控按钮在移动端达到约 44px 最低触控高度。

## 8. 图表验收

ECharts 图表必须分别检查正常、Loading、Empty、No data 与 Error。

双轴图只用于单位不同且关系明确的指标。条形与柱形默认直角，不使用与当前设计语言不一致的圆角柱体。移动端需要重新布局图例、坐标与雷达半径，而不是只缩放 canvas。

## 9. 外部依赖

当前页面依赖：

- ECharts 5.5.1：jsDelivr CDN。
- Iconify：流程 SVG 图标。
- Anime.js：流程动画。
- Unsplash：示例摄影图片。

发布验收必须模拟 CDN 或图片资源不可用的情况。ECharts 已提供失败状态，图片已有失败占位。若项目需要离线、自托管或高可用环境，应将依赖本地化后再发布。

## 10. 浏览器目标

目标为支持现代 CSS Grid、`clamp()`、`aspect-ratio`、`ResizeObserver` 与现代 JavaScript 的浏览器。正式版本应至少人工检查当前主流 Chrome、Safari、Edge 与 Firefox。

毛玻璃依赖 `backdrop-filter`；在不支持该能力的环境中，内容仍必须通过半透明实色背景保持可读。

## 11. 视觉一致性检查

正式发布前逐项确认：

- 六个一级模块大标题字号、字重、起排线一致。
- 二级 H3 标题与说明、用途列位置一致。
- 大模块、小模块、组件、组件内部只使用 `rhythm.css` 的统一节奏。
- 页面背景只承担章节层级，不用大量分割线制造结构。
- 同类按钮、链接、标签、表单字段使用统一状态逻辑。
- 毛玻璃只在需要透出环境时使用。
- 数据卡片保持连续数据带，不出现双重边框。
- 新闻 / 文章 / 案例内部标题层级一致，但允许内容本身依据版式放大。
- 页脚、Hero、CTA 等页面级组件与基础组件使用同一主题令牌。

## 12. 发布流程

1. 在本地通过静态服务器预览：`python3 -m http.server 8080`。
2. 完成上述桌面、平板、手机视口检查。
3. 检查浏览器控制台，不应存在 JavaScript 错误或 404 静态资源。
4. 检查 ECharts、Iconify、Anime.js 与图片失败降级。
5. 检查键盘导航与 Reduced Motion。
6. 提交到 `main`。
7. 等待 GitHub Pages 自动发布。
8. 对线上页面执行一次硬刷新后的最终检查。

如果线上出现视觉回归，优先回滚引发问题的单个语义模块，不要通过新增全局补丁文件覆盖问题。

## 13. 版本规则

建议使用语义化版本：

- Patch：不改变组件 API 的视觉修复、文字修正、兼容修复。
- Minor：新增组件、变体或可选状态，不破坏现有结构。
- Major：改变一级信息架构、CSS 核心令牌、组件 DOM 契约或删除现有公开组件。

V1 之后禁止通过“V2.1 / V2.2 CSS 覆盖层”继续累积历史。版本变化应体现在 Git 提交、Release / Tag 与变更记录中，而不是文件名中。

## 14. 当前已知限制

当前源码结构与 CSS 规则已经完成发布前整理，但本轮没有在执行环境中完成真实浏览器逐像素截图验收。因此，在正式标记 V1.0.0 Release / Tag 前，仍应完成第 5、10、11、12 节的真实浏览器检查。
