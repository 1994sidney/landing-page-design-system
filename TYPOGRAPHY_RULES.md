# 心理健康服务运营平台｜官网排版层级规则

本规则用于首页、心理服务、行业发展、研究实践、平台介绍等官网内容页面。设计系统的语义令牌以 `styles/modules/typography.css` 为事实实现。

## 普通内容模块

普通内容模块统一采用以下四个排版层级：

| 层级 | HTML / 语义 | 字号 | 建议行高 | 字重 |
| --- | --- | ---: | ---: | ---: |
| 模块标题 | `h2` | **34px** | 1.28 | 500 |
| 模块说明 | H2 下的主要说明段落 | **16px** | 1.90 | 400 |
| 模块内小标题 | `h3` | **22px** | 1.45 | 500 |
| 小标题说明 | H3 对应的说明段落 | **14px** | 1.90 | 400 |

对应 CSS 令牌：

```css
--mh-type-section-title: 34px;
--mh-type-section-copy: 16px;
--mh-type-item-title: 22px;
--mh-type-item-copy: 14px;
```

## 使用边界

以上规则用于普通内容模块，目的是让首页与各二级页面形成稳定一致的阅读层级。不要在单个页面中因为版式变化随意把 H2 放大到 40–60px，或把同级 H3 分别设置为 18px、19px、24px。

Hero / 页面首屏 H1 不受普通模块 H2 规则约束；案例引语、数据数字、政策文件标题、日期、眉签、图片说明、按钮和元信息可根据其语义使用独立字号。特殊展示文本需要独立设计时，应明确说明其语义，不要借用 H2 / H3 标签制造视觉大小。

桌面、平板和移动端原则上保持这四个内容字号不变，优先通过列数、行宽、间距和换行调整响应式布局。只有在确有可读性问题并经过单独确认时，才调整字号。

## 当前落地

- 首页通过 `shared/site-typography.css` 统一普通模块的 H2 / H3 与对应说明文本。
- `教育心理服务` 页面使用同一规则，不再为不同模块分别定义一套标题字号。
- 后续新建二级页面应优先使用 Design System 语义令牌，不重新发明页面级字号体系。
